# RUNBOOK.md — 런북 (증상 → 원인 → 조치)

> 어떤 프로젝트에나 흔한 일반 행 + ramen.live 고유 행. 처음 겪는 고유 이슈는 임시 처치 후 `PROGRESS.md`에 기록하고, 반복되면 여기 행으로 승격한다. 불변식 관련 증상은 조치에 INV-n를 병기한다.

| # | 증상 | 흔한 원인 | 조치 |
|---|---|---|---|
| 1 | 환경/의존성 설치 실패 | 버전 불일치, 잘못된 소스/인덱스, 락파일 깨짐 | 환경 가이드대로 재설치, 버전·소스 고정, 락파일 재생성 |
| 2 | 빌드/타입체크 실패 | 타입 불일치, 설정·경로 오류, 의존성 누락 | 에러 위치부터 좁혀 수정 |
| 3 | 테스트 불안정(flaky) | 시간·순서·외부 의존, 시드 미고정 | 시드 고정, 외부 의존 격리(mock), 결정론 확보 |
| 4 | 스냅샷 비결정(해시 불일치) (INV-7) | 정렬 미고정, 타임스탬프 혼입, Map 순서 의존 | 키 정렬 고정, 생성시각 분리, 결정론적 직렬화 |
| 5 | "돌지만 데이터 비어있음" | 도메인 필터 과도, 조인 키 불일치, API 빈 응답 | 필터·조인 키부터, 초소형 픽스처로 단계별 카운트 확인 |
| 6 | 조인 누락(영양/회수 안 붙음) | 품목제조보고번호 표기 차이(공백·형식) | 키 정규화 단일화, 미매칭 건 리포트 |
| 7 | 처리 느림 | 직렬 fetch, 불필요 반복 | 소스별 병렬 fetch, 배치, 핫스팟 확인 |
| 8 | 경계 위반 lint 에러 (INV-3) | 모듈 간 금지된 import (특히 web→ingest/pipeline) | 경계 준수, 공유는 `shared`만 |
| 9 | 공공데이터 OpenAPI 응답 변경/누락 | 대상 API 스키마·필드명·페이지네이션 변경 | 해당 어댑터(계약) 한 곳만 갱신, 변경분 기록(다른 파일 수정 금지) |
| 10 | 환경별 동작 차이(로컬↔CI) | 환경변수·서비스키·버전 차이 | env/시크릿·버전 일치, Actions secret 점검 |
| 11 | 좌표 변환 오류 | EPSG 코드 혼동(2097/5174 ↔ WGS84) | 소스별 좌표계 확인 후 단일 변환기로 통일 |
| 12 | status 단정 오판 (INV-6) | source·confidence 누락, low를 단정 표기 | 필수필드 강제, confidence<high는 '추정' 마킹 |
| 13 | Tier2 데이터가 스냅샷/DB에 혼입 (INV-4) | 영속 경로에서 `PriceQuote` 참조 | 경계 룰로 import 차단, 저장 호출 제거, 정적 검사 |
| 14 | 도메인 필터 오탐/누락 | 키워드만으로 분류 | 식품유형·업태코드 + 보정 리스트 갱신, 오탐 픽스처화 |
| 15 | 빈/공백 PK 제품이 영양·회수 교차오염 (INV-5) | PRDLST_REPORT_NO 결측을 id="" 로 통과 | 정규화/조인 진입 시 `isValidProductId`로 무효 PK 드롭, 빈 키 인덱싱 금지 |
| 16 | 영양값이 0으로 잘못 표시 | 필수 4종 결측을 `?? 0`으로 날조 | 결측 시 nutrition=null(빈칸 미채움, 백서 §5). 단위·비수치는 parseNum이 거름 |
| 17 | 교차환경 산출/해시 불일치 (INV-7) | 정렬에 localeCompare(로케일·ICU 의존) | `compareCodeUnits`(shared) 코드유닛 비교로 통일 |
| 18 | 보정 리스트가 침묵 실패 | 보정 키가 제품 id와 정규화 비대칭(공백) | 보정 키도 `normalizeProductId`로 정규화 후 비교 |
| {{n}} | {{고유 증상}} | {{...}} | {{...}} |

## 행 추가 규칙
같은 이슈를 2회 이상 겪으면 행으로 승격. 형식: 증상(관측 가능) / 가장 흔한 원인 순 / 최소 조치. 불변식 관련이면 INV-n 병기.
